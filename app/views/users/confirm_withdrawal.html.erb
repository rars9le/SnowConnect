<% provide :title, '退会' %>

<div class="container user_form mt-3 mb-3 ">
  <%= render "shared/error_messages" %>

  <h2 class="form_title">退会</h2>

  <%= form_with(model: @user, local: true) do |f| %>

    <div class="alert alert-success" id="flash">
      <div class="container">
        退会をご希望される場合は退会ボタンより実行して下さい。
      </div>
    </div>
    <div class="alert alert-danger" id="flash">
      <div class="container">
        【注意】この処理は取り消すことができません。退会処理後のデータ復旧などは一切できませんのでご注意下さい。
      </div>
    </div>

    <div class="d-flex justify-content-center">
      <%= link_to '退会', withdrawal_user_path(current_user.id), method: :delete, data: {confirm: "本当に退会しますか？"}, class: 'btn btn-danger' %>
    </div> 

  <% end %>
  
</div>
